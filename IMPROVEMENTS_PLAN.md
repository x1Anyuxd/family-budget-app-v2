# 家庭记账应用 v2 - 第二轮改进计划

## 已完成的改进

### 1. 拍照和相册导入功能修复 ✅
- **文件**: `lib/camera-service.ts`
- **改进内容**:
  - 添加了完整的错误处理
  - 改进了权限请求逻辑
  - 增加了资源验证
  - 提高了代码的健壮性

### 2. 记账页面刷新清空数据 ✅
- **文件**: `app/(tabs)/add-transaction.tsx`
- **改进内容**:
  - 添加了 `useFocusEffect` 钩子
  - 每次打开页面时自动清空表单数据
  - 重置所有输入字段到初始状态

## 待完成的改进

### 3. 导出JSON格式功能
- **文件**: `app/(tabs)/records.tsx`
- **需要做的**:
  - 在导出菜单中添加"导出JSON"选项
  - 实现 JSON 格式导出功能
  - 添加文件共享功能
  - 更新 i18n 翻译

### 4. 预算新增"总计"类目
- **文件**: `app/(tabs)/budget.tsx`
- **需要做的**:
  - 在预算列表顶部添加总计卡片
  - 显示所有预算的总额
  - 显示所有支出的总额
  - 显示剩余预算总额

### 5. 收信箱删除超预支提醒
- **文件**: `app/(tabs)/settings.tsx` 和 `lib/budget-context.tsx`
- **需要做的**:
  - 在 budget-context 中添加 deleteMessage 函数
  - 在收信箱中为提醒消息添加删除按钮
  - 支持删除特定的预算告警消息

## 实现建议

由于自动化修改容易引入错误，建议采用以下方法：

1. **逐个功能实现** - 一次只修改一个功能
2. **充分测试** - 每个功能完成后进行 TypeScript 检查
3. **代码审查** - 确保代码质量和一致性
4. **增量提交** - 每个功能完成后提交一次

## 下一步

当您准备好时，我将按照上述计划逐个实现这些功能，并确保所有代码都能通过 TypeScript 检查。
